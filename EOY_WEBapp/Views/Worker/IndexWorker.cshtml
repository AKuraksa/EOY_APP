@model IEnumerable<EOY_WEBapp.Models.WorkerModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Vytvořit nového dělníka</a>
</p>
<table class="table">
    <thead>
        <tr>
          
            <th>
                @Html.DisplayNameFor(model => model.Created)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Updated)
            </th>
            <th>
                @Html.DisplayName("Jméno dělníka")
            </th>
           
            <th>
                @Html.DisplayNameFor(model => model.AuthentificatorID)
            </th>
            <th>
                @Html.DisplayName("Přidělené pracoviště")
             
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
           
            <td>
                @Html.DisplayFor(modelItem => item.Created)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Updated)
            </td>
            <td>
                    @Html.DisplayFor(modelItem => item.WorkerFirstName) @Html.DisplayFor(modelItem => item.WorkerLastName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AuthentificatorID)
            </td>
            <td>
             @*    @{
                        var data = @Html.DisplayFor(modelItem => item.LoggedWorkplace)
                }
                    // @if (!string.IsNullOrWhiteSpace(data))
                    // {
                    //     @Html.Raw("Nepřiřazeno")
                    // } *@
                    // else
                    // {
                        @Html.DisplayFor(modelItem => item.LoggedWorkplace)
                   @*  } *@
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
